{
  "name": "jobagent-browser-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local, headed browser assistant for the AI Job Application Agent. Polls the backend for authorized autofill tasks, fills forms in a visible browser on your own machine, and stops dead at every hard stop.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "playwright": "^1.47.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
